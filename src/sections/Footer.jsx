import { Box, Typography } from "@mui/material";

function Footer() {
    return (
        <Box sx={{ height: '7vh', display: 'flex', justifyContent: 'center', alignItems: 'center', backgroundColor: '#45AAB8'}}>
            <Typography sx={{ color: '#ffffff'}}>
            
            </Typography>
        </Box>
    );
};

export default Footer;